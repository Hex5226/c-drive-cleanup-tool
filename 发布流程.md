---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: 0c7dbeb585f6008cc37be2d80691fbbb_62cfe3c3987511f18cca525400e6dd8f
    ReservedCode1: /dwIP8ZI8yVUwd23AqVkVB9S61VYjmpnRBQFn1H8HGh0MsHAZ6GIP+LDN6xKKdw5n+YJpY6GZFbGs4r08v67dhJEHtSMAuEi7vydsuEA7wtkF+O7YtuB0GS1L181/A89tMz0gqMbT8XTMCwXow/SxncIF57R4IeSO8VvO2e7kLp/2TgoERrxgsxQp5A=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: 0c7dbeb585f6008cc37be2d80691fbbb_62cfe3c3987511f18cca525400e6dd8f
    ReservedCode2: /dwIP8ZI8yVUwd23AqVkVB9S61VYjmpnRBQFn1H8HGh0MsHAZ6GIP+LDN6xKKdw5n+YJpY6GZFbGs4r08v67dhJEHtSMAuEi7vydsuEA7wtkF+O7YtuB0GS1L181/A89tMz0gqMbT8XTMCwXow/SxncIF57R4IeSO8VvO2e7kLp/2TgoERrxgsxQp5A=
---



# CleanC 在线更新发布流程

本工具用 Velopack 做在线更新，主源 Gitee（国内快），备源 GitHub（国外稳）。
Gitee 连不上时程序会自动切 GitHub，两个都连不上才提示失败。

## 一、注册账号

1. **Gitee**：打开 https://gitee.com 注册（主源，国内下载快）
2. **GitHub**：打开 https://github.com 注册（备源）

## 二、创建仓库

两个平台各建一个仓库，名字都叫 `c-drive-cleanup-tool`（公开仓库，Private 不行）。
注意：仓库名就是访问路径，Gitee 的"路径"和 GitHub 的"仓库名"要一致，程序才能正常检查更新。

## 三、生成私人令牌（Token）

### Gitee
1. 登录后点右上角头像 → 设置 → 私人令牌
2. 点"生成新令牌"，勾选 `projects` 权限
3. 生成后**立刻复制保存**（只显示一次）

### GitHub
1. 头像 → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token，勾选 `repo` 权限
3. 生成后**立刻复制保存**

## 四、填入仓库地址和令牌

### 1. 改 updater.py（程序里检查更新用的）
打开 `F:\CleanC_Project\updater.py`，把顶部两行改成你的仓库：

```python
GITEE_REPO = "https://gitee.com/Hex5226/c-drive-cleanup-tool"
GITHUB_REPO = "https://github.com/Hex5226/c-drive-cleanup-tool"
```

### 2. 改发布脚本
- 打开 `发布到Gitee.ps1`，填 Gitee 仓库地址和 Gitee 令牌
- 打开 `发布到GitHub.ps1`，填 GitHub 仓库地址和 GitHub 令牌

## 五、打包

右键 `一键打包.ps1` → 使用 PowerShell 运行。
打包完成后 `Releases` 目录会生成：
- `CleanC安装向导.exe` 安装向导（内嵌 Setup.exe，发给用户装这个，安装时可选路径）
- `CleanC-win-Setup.exe` 安装包（安装向导内部调用的，一般不用单独发给用户）
- `CleanC-win-Portable.zip` 便携版
- `CleanC-3.1.0-full.nupkg` 更新包（自动更新用）
- `RELEASES` 更新清单（自动更新用）

## 六、发布

1. 右键 `发布到Gitee.ps1` → 使用 PowerShell 运行（上传到 Gitee）
2. 右键 `发布到GitHub.ps1` → 使用 PowerShell 运行（上传到 GitHub）

两个都发布，用户在国内走 Gitee，连不上自动切 GitHub。

> **注意**：Gitee 的 Gitea API 和 vpk 不兼容，`发布到Gitee.ps1` 已改用 Gitee 官方 API 直接上传。
> 脚本会自动上传 3 个更新包 + `releases.win.json` + `RELEASES` 元数据文件。
> **元数据文件必须上传**，否则 Gitee 源检查不到更新（只显示"已是最新"）。

## 七、用户安装/更新流程

用户下载 `CleanC安装向导.exe`，双击运行：
- 安装向导会自动检测非 C 盘，默认装到第一个非系统盘下的 `CleanC` 目录（比如 `D:\CleanC`）
- 也可以点"浏览"自己选安装路径
- 两个勾选框：
  - 创建桌面和开始菜单快捷方式（默认勾选）
  - 安装成功后打开 CleanC（默认不勾选）
- 确认路径后点"安装"，自动完成安装

安装完成后默认**不会**自动打开 CleanC（CleanC 需要管理员权限，打开时会自动弹 UAC 确认）。
装好后打开程序点"检查更新"按钮：
- 有新版 → 提示下载 → 下载完自动重启完成更新
- 没新版 → 提示已是最新

## 八、发新版步骤（以后每次更新）

1. 改代码
2. 改 `一键打包.ps1` 里的 `$VERSION`（版本号要递增，如 3.1.0 → 3.2.0）
3. 运行 `一键打包.ps1`
4. 运行 `发布到Gitee.ps1` 和 `发布到GitHub.ps1`
5. 把新的 `CleanC安装向导.exe` 发给用户（`一键打包.ps1` 会自动打包安装向导，不用额外操作）

## 常见问题Q&A

**Q：检查更新提示"Gitee 和 GitHub 都连不上"**
A：先看网络，再确认 updater.py 里的仓库地址填对了没。

**Q：发布脚本报错**
A：确认令牌权限勾对了（Gitee 要 projects，GitHub 要 repo），仓库是公开的。

**Q：用户装的是便携版，能自动更新吗**
A：不能。Velopack 自动更新只支持安装版（Setup.exe）。便携版要更新就重新下载。
