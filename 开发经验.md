---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: 0c7dbeb585f6008cc37be2d80691fbbb_a8df4276987111f18cca525400e6dd8f
    ReservedCode1: UENfw6VgNnhZD7FEt72e1eruTFgnheZAVk16LOqkZJtY2SvraBBGnA6boCnSZqs8zOjM73rvBy4i8oGfAQmoV1hmeQV8vokjW/hU8lHpN+4Ckvb3Im9oBgPgAalkxq41j56okyY9kR457YenhB3KkbV46OTgPDwmWqt1Uc7Oz434/2wBHew7fSbmoRg=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: 0c7dbeb585f6008cc37be2d80691fbbb_a8df4276987111f18cca525400e6dd8f
    ReservedCode2: UENfw6VgNnhZD7FEt72e1eruTFgnheZAVk16LOqkZJtY2SvraBBGnA6boCnSZqs8zOjM73rvBy4i8oGfAQmoV1hmeQV8vokjW/hU8lHpN+4Ckvb3Im9oBgPgAalkxq41j56okyY9kR457YenhB3KkbV46OTgPDwmWqt1Uc7Oz434/2wBHew7fSbmoRg=
---

# CleanC 在线更新功能开发经验

> 记录时间：2026-08-15
> 适用范围：CleanC 项目后续发版、维护、以及任何用 Velopack 做 Windows 桌面应用在线更新的场景

## 一、技术选型

- **Velopack**（vpk 1.2.0，.NET 工具）：Windows 桌面应用在线更新框架，支持原子更新、自动重启
- 排除方案：自研轻量更新（要维护、易出错）、WinGet（用户需会命令行，不适合新手）
- 选型依据：最稳定、最易维护、最适合用户操作

## 二、打包方式（关键）

- **Velopack 要求 PyInstaller 用 `--onedir`（文件夹模式）**，不能用 `--onefile`
- 打包配置：`CleanC_onedir.spec`（COLLECT 模式，hiddenimports 含 CleanC 和 updater）
- 产物在 `dist\CleanC\` 目录
- 打包前先 `Stop-Process` 结束占用旧 exe 的进程，否则 PermissionError
- 打包命令：`pyinstaller --clean --noconfirm CleanC_onedir.spec`，再 `vpk pack`

## 三、Velopack manifest 格式（踩坑最多）

**manifest 字段是 XML 子元素，不是属性！**

错误写法（解析报 "Missing required manifest property: id"）：
```xml
<VelopackAsset id="CleanC" version="3.1.0" ... />
```

正确写法：
```xml
<?xml version="1.0" encoding="utf-8"?>
<VelopackAsset>
  <id>CleanC</id>
  <version>3.1.0</version>
  <title>CleanC</title>
  <mainExe>CleanC.exe</mainExe>
  <os>win</os>
  <channel>win</channel>
</VelopackAsset>
```

必填字段：`id`、`version`（不能是 0.0.0）、`mainExe`（Windows 必填）
可选字段：`title`（默认用 id）、`os`、`channel` 等

## 四、Python API 构造（关键）

`UpdateManager` 需要三个参数，开发模式（未安装）必须显式传 locator：

```python
from velopack import UpdateManager, UpdateOptions, GiteaSource, GithubSource

locator = velopack.VelopackLocatorConfig(
    root_app_dir,          # 应用根目录
    update_exe_path,       # Update.exe 路径
    packages_dir,          # packages 目录
    manifest_path,         # current\CleanC.exe.manifest 路径
    current_binary_dir,    # current 目录
    is_portable,           # False
)
opts = UpdateOptions(False, 0)  # (AllowVersionDowngrade, MaximumDeltasBeforeFallback)
um = UpdateManager(source, opts, locator)
```

- 开发模式（未安装）调 UpdateManager 报 "not properly installed: Could not auto-locate app manifest"，需捕获返回 ("dev", None, None)
- `check_for_updates()` 返回 None 表示无更新
- `download_updates(info)` 下载到 packages 目录
- `apply_updates_and_restart(info)` 应用更新并重启（会真的重启程序，测试环境慎用）

## 五、Gitee 发布（踩坑最多）

### 1. vpk upload gitea 与 Gitee 不兼容
- `vpk upload gitea` 调 Gitee 的 Gitea API 接口，Gitee 返回 404
- **必须改用 Gitee 官方 API 直接上传**（`发布到Gitee.ps1` 已实现）

### 2. 元数据文件必须上传（关键！）
Gitee release 除了 3 个更新包，**必须上传 `releases.win.json` 和 `RELEASES`**，否则 Velopack 检查不到更新（只显示"已是最新"）。

### 3. Gitee API 流程
```powershell
# 创建 release（tag = 版本号）
POST https://gitee.com/api/v5/repos/{owner}/{repo}/releases?access_token=TOKEN
# 上传资产（逐个上传，已存在的会报错，跳过即可）
POST https://gitee.com/api/v5/repos/{owner}/{repo}/releases/{release_id}/attach_files?access_token=TOKEN&name=文件名
```

### 4. 版本号从 releases.win.json 解析
```powershell
$json = Get-Content "$RELEASES_DIR\releases.win.json" -Raw | ConvertFrom-Json
$VERSION = $json.Assets[0].Version
```

## 六、GitHub 发布

- `vpk upload github` 直接可用（无需手动 API）
- 命令：`vpk upload github --outputDir $RELEASES_DIR --repoUrl $GITHUB_REPO --token $GITHUB_TOKEN --publish True`
- 自动上传 3 个更新包 + releases.win.json + RELEASES

## 七、PowerShell 脚本编码（重要）

- **PowerShell 5.1 默认按 GBK 读取 .ps1，UTF-8 无 BOM 的中文会乱码报 ParserError**
- 解决方案：保存为**带 BOM 的 UTF-8**
```powershell
$content = [System.IO.File]::ReadAllText("脚本.ps1", [System.Text.Encoding]::UTF8)
[System.IO.File]::WriteAllText("脚本.ps1", $content, [System.Text.UTF8Encoding]::new($true))
```
- 语法检查：
```powershell
$errors = $null
[System.Management.Automation.Language.Parser]::ParseFile("脚本.ps1", [ref]$null, [ref]$errors)
```

## 八、双源 fallback 设计

- 主源 Gitee（国内快），备源 GitHub（国外稳）
- updater.py 里 sources 列表按顺序尝试，Gitee 失败自动切 GitHub
- 两个都失败才报错
- 仓库名/路径两个平台保持一致（`c-drive-cleanup-tool`），URL 才统一

## 九、测试方法（模拟安装环境）

1. 解压 nupkg 到 `test_install\current\`（lib\app 内容）
2. 复制 Update.exe 到 test_install（路径：`C:\Users\Admin\.dotnet\tools\.store\vpk\1.2.0\vpk\1.2.0\vendor\update.exe`）
3. packages 目录放 nupkg
4. current 目录放 CleanC.exe.manifest（子元素格式）
5. 用 VelopackLocatorConfig 显式构造 locator 测 check_for_updates
6. 改 manifest 版本号模拟旧版（3.0.0），验证能发现新版（3.1.0）

## 十、发新版流程（以后每次更新）

1. 改代码
2. 改 `一键打包.ps1` 里的 `$VERSION`（版本号递增）
3. 运行 `一键打包.ps1`（PyInstaller onedir + vpk pack）
4. 运行 `发布到Gitee.ps1`（Gitee API 上传，含元数据）
5. 运行 `发布到GitHub.ps1`（vpk upload github）
6. 把新的 `CleanC-win-Setup.exe` 发给用户

## 十一、安全提醒

- 权限最小化：Gitee 只勾 `projects`，GitHub 只勾 `repo`

## 十二、已知限制

- 便携版（Portable.zip）不支持自动更新，只有安装版（Setup.exe）支持
- "应用更新"环节（apply_updates_and_restart）需真实安装后验证
*（内容由AI生成，仅供参考）*
